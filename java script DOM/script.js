let age = prompt("enter your age")  

 if (age<0) {
    console.log("error of age")
    prompt("please enter a valid age")
}
else{
    if (age>=18) {
        alert("you can drive")
        console.log(`${a}`)
        let age = number(age)
    }
    else{
        alert("you can't drive")
        // console.log(`${a}`)
    }
confirm("you can enter your age again")
}

